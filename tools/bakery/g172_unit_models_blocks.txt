THE FOUR DESERT-FLORA ENTRIES, in the exact text form support/unit_models.json uses.

Each block is INSERTED IMMEDIATELY BEFORE the anchor line named above it, so the four
edits are independent of each other and none of them touches the end of the file, where
entries are being appended. Each anchor occurs exactly once at the start of a line.
The same content is in g172_unit_models_additions.json as data.

==== T04D : insert before the line     "T05": {

  "T04D": {
    "confidence": "high",
    "dl_rom": "0x00FE360",
    "evidence": "LOOKED UP, not inferred. This is the mesh the DESERT theater draws in place of T01, T02, T03 and T04, and these four meshes are why nineteen model-table slots looked orphaned. THE REMAP: the draw-command enqueue at RAM 0x8004A420 (resident ROM 0x04B020) reads the theater word at RAM 0x80097150 and, when it is ZERO, adds 97 to any model id in [103,121]. 0x8004A430 lw $v0,0x7150($v0); 0x8004A454 bnez $v0 skips the whole arm; 0x8004A45C slti $v0,$s0,0x67 skips ids below 103; 0x8004A468 slti $v0,$s0,0x7A; 0x8004A470 addiu $s0,$s0,0x61 sits in a BRANCH-LIKELY delay slot, so it executes only when that branch is taken and the input range is exactly [103,121]. THEATER 0 IS DESERT, read off the loader's own filename switch at RAM 0x801E5190: 0 selects DESERT.TL4 and DESERT.TL8 (strings at RAM 0x801C3C14 and 0x801C3C20), 2 selects TEMPERAT.TL4 and TEMPERAT.TL8 (0x801C3C2C, 0x801C3C3C), and anything else reaches the printf 'Illegal theater type: %d' at 0x801C3C4C. The theater id is the word at RAM 0x800935E0; the only writer of 0x80097150 is RAM 0x800485B0, which copies it. THE IDS ARE THE TREES: the 32 TerrainTypeClass registrations (ctor RAM 0x801EAC0C, jals from ROM 0x18CEE0 to 0x18DD70, function entry ROM 0x18CE2C) pass three per-theater model ids on the stack at sp+0x38 desert, sp+0x3C temperate, sp+0x40 winter, and $s2 is set to -1 at ROM 0x18CE6C, which is what an unused theater field holds. Read end to end they give T01=103 through T08=110, 111 unused, T09=112 through T18=121, SPLIT2/SPLIT3=122 and ROCK1..ROCK7=124..130. T04's own registration is the jal at ROM 0x18D038: sp+0x38 = 106 desert, sp+0x3C and sp+0x40 = -1, so T04 exists in DESERT only and in desert it draws id 106 + 97 = 203. THE ARITHMETIC CLOSES ON ITSELF: model slot = id + 10, and slots 210..228 of the primary model table (base ROM 0x9A598, stride 16, node pointer at +0x0C) hold exactly FOUR distinct scene-graph nodes and nothing else, 0x801B46C8 over slots 210-213, 0x801B46E4 over 214-218, 0x801B4700 over 219-222 and 0x801B471C over 223-228. That is the whole output range the remap can produce and no more. Slot 213 is id 203, node RAM 0x801B46C8, display list 0x00FE360. Slots 210, 211 and 212 share that same node and are the desert forms of T01, T02 and T03, whose desert fields are -1, so no shipped scenario can reach them. WHAT IT IS: a saguaro cactus with two raised arms, plus its own ground shadow patch. Extent 1036 x 1103 x 630 mesh units, which is 1.01 x 1.08 x 0.62 world cells at the renderer's cell of 1024 model units. Ten of its twelve triangles lie at z = 0 EXACTLY, so the cactus body is a single upright plane whose silhouette is cut by GEOMETRY rather than by texture alpha; its 16x4 body texture has zero alpha-0 texels and the baker therefore classes those ten MODE_OPAQUE, which is the data and not a mis-classification. The other two are the shadow patch and bake MODE_SHADOW off their I-format 8x16 texture, exactly as every tree's ground patch already does. The four display lists sit contiguously in ROM immediately before ROCK1 at 0x00FEE70 and their textures sit in the same block as the tree art. WHICH MAPS: counted over the 100 shipped mission INIs (46 DESERT, 52 TEMPERATE, 2 WINTER), the desert theater places only T04, T08, T09, T18, SPLIT3 and ROCK1..ROCK7 in its [TERRAIN] sections, and T04 lands on 32 cells in 6 of them. HOW IT IS BOUND: the baker resolves the INI name T04 to this mesh when the scenario's theater is DESERT and to dl_012C968 otherwise, which is the bake-time resolution of the cartridge's run-time +97. The type table is keyed by INI name, so the renderer needs no change and the pack format does not move. For scale beside it, the temperate T04 billboard is 2.28 x 1.24 x 2.03 cells, so the cactus is roughly half the height of the tree it replaces.",
    "mesh": "dl_00FE360",
    "model_index": 213,
    "n64_model_index": 203,
    "node_ram": "0x801B46C8",
    "segment": "GameModelsGeometry",
    "theaters": [
      "desert"
    ],
    "tris": 12
  },

==== T08D : insert before the line     "T09": {

  "T08D": {
    "confidence": "high",
    "dl_rom": "0x00FE660",
    "evidence": "LOOKED UP, not inferred. This is the mesh the DESERT theater draws in place of T05, T06, T07 and T08, and these four meshes are why nineteen model-table slots looked orphaned. THE REMAP: the draw-command enqueue at RAM 0x8004A420 (resident ROM 0x04B020) reads the theater word at RAM 0x80097150 and, when it is ZERO, adds 97 to any model id in [103,121]. 0x8004A430 lw $v0,0x7150($v0); 0x8004A454 bnez $v0 skips the whole arm; 0x8004A45C slti $v0,$s0,0x67 skips ids below 103; 0x8004A468 slti $v0,$s0,0x7A; 0x8004A470 addiu $s0,$s0,0x61 sits in a BRANCH-LIKELY delay slot, so it executes only when that branch is taken and the input range is exactly [103,121]. THEATER 0 IS DESERT, read off the loader's own filename switch at RAM 0x801E5190: 0 selects DESERT.TL4 and DESERT.TL8 (strings at RAM 0x801C3C14 and 0x801C3C20), 2 selects TEMPERAT.TL4 and TEMPERAT.TL8 (0x801C3C2C, 0x801C3C3C), and anything else reaches the printf 'Illegal theater type: %d' at 0x801C3C4C. The theater id is the word at RAM 0x800935E0; the only writer of 0x80097150 is RAM 0x800485B0, which copies it. THE IDS ARE THE TREES: the 32 TerrainTypeClass registrations (ctor RAM 0x801EAC0C, jals from ROM 0x18CEE0 to 0x18DD70, function entry ROM 0x18CE2C) pass three per-theater model ids on the stack at sp+0x38 desert, sp+0x3C temperate, sp+0x40 winter, and $s2 is set to -1 at ROM 0x18CE6C, which is what an unused theater field holds. Read end to end they give T01=103 through T08=110, 111 unused, T09=112 through T18=121, SPLIT2/SPLIT3=122 and ROCK1..ROCK7=124..130. T08's own registration is the jal at ROM 0x18D208, and it is the only tree that carries the SAME id 110 in all three theater fields. That is what makes this binding theater dependent rather than a plain correction: in TEMPERATE 110 gives slot 120 and the leafy billboard dl_012D570, and in DESERT the same 110 becomes 207 and gives slot 217 and this mesh. THE ARITHMETIC CLOSES ON ITSELF: model slot = id + 10, and slots 210..228 of the primary model table (base ROM 0x9A598, stride 16, node pointer at +0x0C) hold exactly FOUR distinct scene-graph nodes and nothing else, 0x801B46C8 over slots 210-213, 0x801B46E4 over 214-218, 0x801B4700 over 219-222 and 0x801B471C over 223-228. That is the whole output range the remap can produce and no more. Slot 217 is id 207, node RAM 0x801B46E4, display list 0x00FE660. Slots 214, 215 and 216 share that node as the desert forms of T05, T06 and T07 (desert field -1, unreachable), and slot 218 is id 208, pre-remap 111, which no terrain type claims at all. WHAT IT IS: a wide, low desert scrub tree with a brown speckled canopy. Extent 2087 x 1247 x 1424 mesh units, which is 2.04 x 1.22 x 1.39 world cells at the renderer's cell of 1024 model units. Six of its eight triangles are alpha-keyed CUTOUT billboards on three intersecting planes, the same construction and the same baked triangle modes the temperate trees use; the other two are the flat y = 0 shadow patch and bake MODE_SHADOW. Nothing new is needed to draw it. The four display lists sit contiguously in ROM immediately before ROCK1 at 0x00FEE70 and their textures sit in the same block as the tree art. WHICH MAPS: counted over the 100 shipped mission INIs (46 DESERT, 52 TEMPERATE, 2 WINTER), the desert theater places only T04, T08, T09, T18, SPLIT3 and ROCK1..ROCK7 in its [TERRAIN] sections, and T08 is the commonest object in the theater at 558 cells in 44 of the 46 desert missions. It is also placed on 79 temperate cells in 19 missions, which is exactly why its binding must follow the theater and not simply be replaced. HOW IT IS BOUND: the baker resolves the INI name T08 to this mesh when the scenario's theater is DESERT and to dl_012D570 otherwise, which is the bake-time resolution of the cartridge's run-time +97. The type table is keyed by INI name, so the renderer needs no change and the pack format does not move. This is the one entry of the four whose temperate binding still matters, so a bake that swapped it unconditionally would break 19 temperate maps.",
    "mesh": "dl_00FE660",
    "model_index": 217,
    "n64_model_index": 207,
    "node_ram": "0x801B46E4",
    "segment": "GameModelsGeometry",
    "theaters": [
      "desert"
    ],
    "tris": 8
  },

==== T09D : insert before the line     "T10": {

  "T09D": {
    "confidence": "high",
    "dl_rom": "0x00FE918",
    "evidence": "LOOKED UP, not inferred. This is the mesh the DESERT theater draws in place of T09, T10, T11 and T12, and these four meshes are why nineteen model-table slots looked orphaned. THE REMAP: the draw-command enqueue at RAM 0x8004A420 (resident ROM 0x04B020) reads the theater word at RAM 0x80097150 and, when it is ZERO, adds 97 to any model id in [103,121]. 0x8004A430 lw $v0,0x7150($v0); 0x8004A454 bnez $v0 skips the whole arm; 0x8004A45C slti $v0,$s0,0x67 skips ids below 103; 0x8004A468 slti $v0,$s0,0x7A; 0x8004A470 addiu $s0,$s0,0x61 sits in a BRANCH-LIKELY delay slot, so it executes only when that branch is taken and the input range is exactly [103,121]. THEATER 0 IS DESERT, read off the loader's own filename switch at RAM 0x801E5190: 0 selects DESERT.TL4 and DESERT.TL8 (strings at RAM 0x801C3C14 and 0x801C3C20), 2 selects TEMPERAT.TL4 and TEMPERAT.TL8 (0x801C3C2C, 0x801C3C3C), and anything else reaches the printf 'Illegal theater type: %d' at 0x801C3C4C. The theater id is the word at RAM 0x800935E0; the only writer of 0x80097150 is RAM 0x800485B0, which copies it. THE IDS ARE THE TREES: the 32 TerrainTypeClass registrations (ctor RAM 0x801EAC0C, jals from ROM 0x18CEE0 to 0x18DD70, function entry ROM 0x18CE2C) pass three per-theater model ids on the stack at sp+0x38 desert, sp+0x3C temperate, sp+0x40 winter, and $s2 is set to -1 at ROM 0x18CE6C, which is what an unused theater field holds. Read end to end they give T01=103 through T08=110, 111 unused, T09=112 through T18=121, SPLIT2/SPLIT3=122 and ROCK1..ROCK7=124..130. T09's own registration is the jal at ROM 0x18D288: sp+0x38 = 112 desert, the other two fields -1, so in desert it draws id 112 + 97 = 209. THE ARITHMETIC CLOSES ON ITSELF: model slot = id + 10, and slots 210..228 of the primary model table (base ROM 0x9A598, stride 16, node pointer at +0x0C) hold exactly FOUR distinct scene-graph nodes and nothing else, 0x801B46C8 over slots 210-213, 0x801B46E4 over 214-218, 0x801B4700 over 219-222 and 0x801B471C over 223-228. That is the whole output range the remap can produce and no more. Slot 219 is id 209, node RAM 0x801B4700, display list 0x00FE918. Slots 220, 221 and 222 share that node as the desert forms of T10, T11 and T12, whose desert fields are -1. WHAT IT IS: a small twin-stemmed cactus with one short arm, plus its ground shadow. Extent 1045 x 987 x 629 mesh units, which is 1.02 x 0.96 x 0.61 world cells at the renderer's cell of 1024 model units. Six of its eight triangles lie at z = 0 EXACTLY and carry the same 16x4 body texture with no alpha-0 texel, so like the saguaro the body is a single geometry-cut plane and bakes MODE_OPAQUE. The other two are the shadow patch and bake MODE_SHADOW. The four display lists sit contiguously in ROM immediately before ROCK1 at 0x00FEE70 and their textures sit in the same block as the tree art. WHICH MAPS: counted over the 100 shipped mission INIs (46 DESERT, 52 TEMPERATE, 2 WINTER), the desert theater places only T04, T08, T09, T18, SPLIT3 and ROCK1..ROCK7 in its [TERRAIN] sections, and T09 lands on 29 cells in 8 of them. HOW IT IS BOUND: the baker resolves the INI name T09 to this mesh when the scenario's theater is DESERT and to dl_012C2C0 otherwise, which is the bake-time resolution of the cartridge's run-time +97. The type table is keyed by INI name, so the renderer needs no change and the pack format does not move. The temperate mesh it displaces, dl_012C2C0, is T02's tree: before this binding a desert T09 drew a temperate conifer.",
    "mesh": "dl_00FE918",
    "model_index": 219,
    "n64_model_index": 209,
    "node_ram": "0x801B4700",
    "segment": "GameModelsGeometry",
    "theaters": [
      "desert"
    ],
    "tris": 8
  },

==== T18D : insert before the line     "TC01": {

  "T18D": {
    "confidence": "high",
    "dl_rom": "0x00FEBD8",
    "evidence": "LOOKED UP, not inferred. This is the mesh the DESERT theater draws in place of T13 through T18, and these four meshes are why nineteen model-table slots looked orphaned. THE REMAP: the draw-command enqueue at RAM 0x8004A420 (resident ROM 0x04B020) reads the theater word at RAM 0x80097150 and, when it is ZERO, adds 97 to any model id in [103,121]. 0x8004A430 lw $v0,0x7150($v0); 0x8004A454 bnez $v0 skips the whole arm; 0x8004A45C slti $v0,$s0,0x67 skips ids below 103; 0x8004A468 slti $v0,$s0,0x7A; 0x8004A470 addiu $s0,$s0,0x61 sits in a BRANCH-LIKELY delay slot, so it executes only when that branch is taken and the input range is exactly [103,121]. THEATER 0 IS DESERT, read off the loader's own filename switch at RAM 0x801E5190: 0 selects DESERT.TL4 and DESERT.TL8 (strings at RAM 0x801C3C14 and 0x801C3C20), 2 selects TEMPERAT.TL4 and TEMPERAT.TL8 (0x801C3C2C, 0x801C3C3C), and anything else reaches the printf 'Illegal theater type: %d' at 0x801C3C4C. The theater id is the word at RAM 0x800935E0; the only writer of 0x80097150 is RAM 0x800485B0, which copies it. THE IDS ARE THE TREES: the 32 TerrainTypeClass registrations (ctor RAM 0x801EAC0C, jals from ROM 0x18CEE0 to 0x18DD70, function entry ROM 0x18CE2C) pass three per-theater model ids on the stack at sp+0x38 desert, sp+0x3C temperate, sp+0x40 winter, and $s2 is set to -1 at ROM 0x18CE6C, which is what an unused theater field holds. Read end to end they give T01=103 through T08=110, 111 unused, T09=112 through T18=121, SPLIT2/SPLIT3=122 and ROCK1..ROCK7=124..130. T18's own registration is the jal at ROM 0x18D6B8: sp+0x38 = 121 desert, the other two fields -1, so in desert it draws id 121 + 97 = 218, the top of the remap's range. THE ARITHMETIC CLOSES ON ITSELF: model slot = id + 10, and slots 210..228 of the primary model table (base ROM 0x9A598, stride 16, node pointer at +0x0C) hold exactly FOUR distinct scene-graph nodes and nothing else, 0x801B46C8 over slots 210-213, 0x801B46E4 over 214-218, 0x801B4700 over 219-222 and 0x801B471C over 223-228. That is the whole output range the remap can produce and no more. Slot 228 is id 218, node RAM 0x801B471C, display list 0x00FEBD8, and it is the LAST slot of the primary model table: slot 229's node pointer is zero and the bytes after it parse at a different stride. Slots 223 to 227 share that node as the desert forms of T13 to T17, whose desert fields are -1. WHAT IT IS: the largest desert scrub tree, a broad brown canopy over a short trunk. Extent 2980 x 1543 x 1854 mesh units, which is 2.91 x 1.51 x 1.81 world cells at the renderer's cell of 1024 model units. Six of its eight triangles are alpha-keyed CUTOUT billboards on three intersecting planes and the other two are the flat y = 0 shadow patch, which bakes MODE_SHADOW. Same construction as the temperate trees. The four display lists sit contiguously in ROM immediately before ROCK1 at 0x00FEE70 and their textures sit in the same block as the tree art. WHICH MAPS: counted over the 100 shipped mission INIs (46 DESERT, 52 TEMPERATE, 2 WINTER), the desert theater places only T04, T08, T09, T18, SPLIT3 and ROCK1..ROCK7 in its [TERRAIN] sections, and T18 lands on 320 cells in 44 of them, second only to T08. HOW IT IS BOUND: the baker resolves the INI name T18 to this mesh when the scenario's theater is DESERT and to dl_012C660 otherwise, which is the bake-time resolution of the cartridge's run-time +97. The type table is keyed by INI name, so the renderer needs no change and the pack format does not move. The temperate mesh it displaces, dl_012C660, is T03's tree.",
    "mesh": "dl_00FEBD8",
    "model_index": 228,
    "n64_model_index": 218,
    "node_ram": "0x801B471C",
    "segment": "GameModelsGeometry",
    "theaters": [
      "desert"
    ],
    "tris": 8
  },
