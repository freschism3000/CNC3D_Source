# Generated by export.py: 2,000-odd files, ~10 MB, all of it derivable from the pack
# and the ROM in one command. The reason the OLD viewer had to be thrown away was that
# only its output survived and its generator did not, so what is tracked here is the
# generator, never the output.
public/data/
